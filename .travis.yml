language: ruby

services:
  - docker

before_install:
  - docker build -t carlad/sinatra .
  - docker run -d -p 80:8080 carlad/sinatra
  - docker ps -a
deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master
